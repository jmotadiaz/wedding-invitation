---
interface Props {
	extraClass?: string;
}

const { extraClass } = Astro.props;
---

<section x-data="{ shown: false }" x-intersect.threshold.20.once="shown = true" class=`h-screen lg:px-20 px-5 flex flex-col justify-center items-center snap-start ${extraClass}`>
  <slot />
</section>
