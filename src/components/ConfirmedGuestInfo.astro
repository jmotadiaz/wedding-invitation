---
import type { Guest } from "../domain/Guest";

export interface Props {
	guest: Guest;
}

const { guest } = Astro.props;
---
<h3 class="text-xl mb-2 text-center w-full">Invitación confirmada</h3>
{
  guest.confirmedAttendees === 1 ? (
    <p>Se confirmó 1 asistente</p>
  ) : (
    <p>Se confirmaron {guest.confirmedAttendees} asistentes</p>
  )
}

<!-- Info about bus seats and bus stops -->
{!!guest.allergies &&
    <div class="border-b-2 border-gray-200 w-1/3 mx-auto my-5"></div>
    <h4 class="font-semibold mb-1">Alergias o/y condición alimentaria</h4>
    <p class="text-sm">{guest.allergies}</p>
}

<!-- Info about bus seats and bus stops -->
{!!guest.bus &&
    <div class="border-b-2 border-gray-200 w-1/3 mx-auto my-5"></div>
    <h4 class="font-semibold mb-1">Traslado</h4>
    <p class="text-sm"><span class="font-semibold">Parada:</span>{guest.busStop}</p>
    <p class="text-sm"><span class="font-semibold">Asientos:</span>{guest.busSeats}</p>
}

<button
    type="submit"
    class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-md w-full mt-5"
    hx-get={`/${guest.uuid}/api/edit-confirm`}
    hx-target="#confirm"
    hx-swap="innerHTML transition:true"
  >
  Editar confirmación
</button>
