---
interface Props {
	extraClass?: string;
}

const { extraClass } = Astro.props;
---

<section
  x-data="{ shown: false }"
  x-intersect.threshold.50.once="shown = true"
  class=`page h-screen lg:px-20 px-5 flex flex-col justify-center items-center text-center snap-center snap-always ${extraClass ?? ""}`
>
  <slot />
</section>
